[global]
    monitor = 0
    follow = mouse
    indicate_hidden = yes
    shrink = no
    separator_height = 1
    padding = 10
    horizontal_padding = 10
    frame_width = 0
    frame_color = "#232323"
    separator_color = frame
    sort = yes
    idle_threshold = 120
    font = Noto Sans CJK SC 15
    line_height = 0
    markup = full
    format = "<span><b>%s %p</b></span>\n%b"
    alignment = left
    show_age_threshold = 60
    word_wrap = yes
    ellipsize = middle
    ignore_newline = no
    stack_duplicates = true
    hide_duplicate_count = false
    show_indicators = yes
    icon_position = left
    max_icon_size = 70
    icon_path = /usr/share/icons/ePapirus/32x32/apps
    sticky_history = yes
    history_length = 20
    always_run_script = true
    corner_radius = 0
    force_xinerama = false
    mouse_left_click = context
    mouse_middle_click = close_all
    mouse_right_click = close_current
    dmenu = "/home/rongzi/.config/ocr_client/ocr_dmenu.sh"
    # geometry = "320x6-20+35"
    startup_notification = false
    # verbosity = mesg
    # notification_height = 15

## Section shortcuts is deprecated. ##
# [shortcuts]
#     close = ctrl+space
#     history = ctrl+shift+space
#     context = ctrl+shift+period

[urgency_low]
    # IMPORTANT: colors have to be defined in quotation marks.
    # Otherwise the '#' and following  would be interpreted as a comment.
    background="#373842"
    foreground="#f5f5f5"
    timeout = 30

[urgency_normal]
    background="#3a3a3a"
    foreground="#f5f5f5"
    timeout = 30

[urgency_critical]
    background="#3a3a3a"
    foreground="#f5f5f5"
    timeout = 30
