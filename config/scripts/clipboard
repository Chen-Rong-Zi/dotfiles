# gpaste-client --oneline | awk '{$1="";print}' | sed 's/^ //g'| rofi -dmenu -config ~/.config/rofi/windows_tasks.rasi | xsel -ib 
gpaste-client --oneline | awk '{$1="";print}' | sed 's/^ //g'| rofi -dmenu -config ~/.config/rofi/windows_tasks.rasi
